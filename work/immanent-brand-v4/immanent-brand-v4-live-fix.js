(function () {
  const data = window.IMMANENT_BRAND_V4_DATA;
  if (!data || !data.home || !data.home.hero || !data.home.hero.titleLines) return;

  data.home.hero.titleLines.en = ["One pomelo,", "two ways", "to experience it."];
})();
